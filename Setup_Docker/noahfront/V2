docker build -t arnaudlandry/noahfront:version2 .

docker run -dit -h noahfront --name noahfront -p 8000:8000 arnaudlandry/noahfront:version2

docker exec -ti noahfront powershell

docker stop $(docker ps -a -q)
docker rm $(docker ps -a -q)